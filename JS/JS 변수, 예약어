변수 

데이터를 저장하고 참조(사용)하는 데이터의 이름
var(var는 되도록 안사용하는게 좋음 권장사항이 아님)

let 

const 

// 재사용이 가능
// 변수 선언!

let a = 2; 
(let이라는 키워드를 이용해서 변수 a 라는 이름을 명시하고 2라는 값을 넣는 행위를 변수를 선언한다라고함)
let b = 5;

console.log(a + b); // 7
console.log(a - b); // -3
console.log(a * b); // 10
console.log(a / b); // 0.4


// 값(데이터)의 재할당 가능!
let a = 12;
console.log(a); // 12

a = 999; 
console.log(a); // 999

// 값(데이터)의 재할당 불가!
const a = 12;
console.log(a); // 12

a = 999;
console.log(a); // TypeError: Assignment to constant variable

거의 대부분은 const 를 사용함
하지만 재할당을 해야하는 경우엔 const 를 let 으로 변경해줌.

예약어 

특별한 의미를 가지고있어, 변수나 함수 이름 등으로 사용할 수 없는 단어

let this = 'Hello!'; // SyntaxError
let if = 123; // SyntaxError
let break = true; // SyntaxError
