함수 

특정 동작(기능)을 수행하는 일부 코드의 집합(부분)
function

// 함수 선언 helloFunc() 이라는 함수를 만드는것 
function helloFunc() {
 // 실행 코드
 console.log(1234);
}

// 함수 호출 
helloFunc(); // 1234
(함수를 실행한다)

function returnFunc {
 return 123;
}

let a = returnFunc();

console.log(a); // 123
