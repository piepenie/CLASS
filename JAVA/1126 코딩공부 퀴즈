Q1. 새로운 자료형을 만드는 모델링 도구를 무엇이라고하는가?
>>  Class 

Q2. 책이라는 자료형이 Book 으로 만들어져있다. 책 한권의 데이터를 저장하기 위한 변수를 선언 하시요.
>> Book b; 

Q3. 객체를 생성할 때 사용하는 연산자와 메서드를 무엇이라고 하는가?
>> new, 생성자 메서드

Q4. 클래스 안에 만들어지는 변수를 무엇이라고 하는가?(3가지로 쓰시오).
>> 속성, 멤버변수, 프로퍼티

이 세 용어는 같은 의미로 사용됩니다. 모두 클래스 내부에 정의된 변수(필드)를 가리킵니다.
다만, 문맥에 따라 선호되는 표현이 다를 수 있습니다:
속성(attribute): 객체 지향 프로그래밍에서 객체가 가지는 데이터나 특징을 가리킬 때 자주 사용됩니다.
멤버 변수(member variable): 클래스에 속한 변수라는 의미로, 객체가 생성되면 각 인스턴스에 독립적으로 존재합니다.
프로퍼티(property): 속성을 조금 더 고급스럽게 표현한 단어로, 특히 UI 프레임워크나 특정 언어에서는 프로퍼티(getter/setter 메서드)를 통해 속성을 관리하는 경우 사용됩니다.

Q5. 클래스 안에 있는 title 이라는 멤버변수를 참조하는 연산자는 무엇인가?
>> dot(도트 연산자, 참조연산자)
ex) b.title = ""; (단 title은 string 이다)

Q6. 영화 한 편을 저장하기 위한 인스턴스 변수를 만드세요.(영화 자료형은 Movie다)
>> Movie m = new Movie();
