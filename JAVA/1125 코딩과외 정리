public void main(String[] args){
 int number = 1;
}

stack frame 
지역변수
반환주소값
매게변수 이 3개를 묶어서 스택프레임이라고 한다.
스택프레임안에 
지역변수 반환주소값 매게 변수가 쌓이는 순서는 중요하지않다.

큐 vs Stack 
자료구조 

큐 >> 
선입 선출구조

Stack >>
선입 후출구조 

stack overflower 
>> 오류 재귀와 스택오버플러우 

Runtime 메모리 

기본형은 null 값을 수용하지않는다.
>> 컴파일에서 받아주지않는다.

참조형은 null 값을 수용한다.

uuid 를 쓰는 순간
유저를 특정할수가없다.
분산db 환경

장단점
pk를 숫자로 indexing 이 쉽다.
uuid는 문자열 덩어리라 검색속도가 늦어진다.
 
