함수
jvm 멀티 스레드 메모리 구조 
Thread 와 Process 의 차이 

Process >> OS 로부터 리소스를 할당받아서 실행중인 
프로그램을 프로세스라고한다
자원을 할당 받은 상태로 실행중인 상태

Thread >> Process 의 단위.

프로그램 ?
=> 코드 덩어리

프로세스가 cpu에서 실행되어야되고
프로세스 안에 thread 라는 단위가 실행된다.

백발백중에 적혀있음

thread 의 개수 차이점

싱글 vs 멀티 ? 
4코어 cpu 같은경우는 4스레드 

멀티 스레드 = 병렬처리 >> 동시에 작업한다 
멀티 스레드의 단점
프로세스가 대기하는 시간이 생길수 있다. 

싱글스레드는 계속 코어를 차지할수있어서 
과정이 계속 진행될 수 있다.

I/O 작업
>> cpu의 작업이 멈춘다.

cpu clock 스레드를 갈아끼어주는 시간단위 
context switching 
오버헤드 >> 불필요한 작업 쓸데없는 작업 
간접적인 처리시간 

커널 스레드, 유저 스레드 

OS 에서는 유저모드와 커널 모드가 있다. 
유저모드는 커널모드한테 부탁해야한다.
유저모드의 스레드와 커널모드의 스레드는 다르다
유저모드의 스레드가 커널모드의 스레드에 들어가서
실행되는구조.

한정된 자원에 여러개의 요청
레이스 컨디션(= 경쟁 상태)

메서드이름과 파라미터 요청

리턴 타입 

다른클래스에서도 쓸수있는가?
내클래스에만 쓸수잇는가 
public
private 

과제.
Java 컴파일을 먼저 찾아보고?
Runtime 이 어떤 의미를 가지는지 
