번외로 그 윈도우 실행될때마다 자바 업데이트 ?
JRE 개념임. OS 에 깔리는 JRE와
JDK 폴더에 있는 JRE는 완전히 다른개념!

클래스 >> 참조형 변수 >> 주소값에 
User user1 = new User("Alice", 25);
에서 인스턴스 값이 heap 메모리에 들어간다는것?
그리고 user1 은 그냥 주소값만 들고있다가.

생성자 오버로딩(Constructor overloading)
같은 이름을 메서드로 파라미터를 다르게해서 
사용할 수 있게 해주는 것 

여러개의 데이터를 다루는 배열(array) 
=리스트구조 
=선형구조
=연속적인구조
데이터를 이동하는 배열

동일한 데이터. 

자바 연산자에 대해서 공부 해올것

String[] 배열 >> 따로 이해해야되고
(args >> main 의 args 다른개념)

생성자 
>> 클래스를 인스턴스로 만드는 방법을 정의하는것
