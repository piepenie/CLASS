for(int i = 0; i < a.length; i++) {
	a[i] = 10;
}

Q. 여기서 i = 주소값이라는거야?
A. 완전 다름. 하지만 상관관계는 있음.

인덱스와 주소값은 엄연히 다른 개념이지만, 서로 상관관계가 있습니다.

커널 스레드, 유저 스레드 

OS 에서는 유저모드와 커널 모드가 있다. 
유저모드는 커널모드한테 부탁해야한다.
유저모드의 스레드와 커널모드의 스레드는 다르다
유저모드의 스레드가 커널모드의 스레드에 들어가서
실행되는구조.

한정된 자원에 여러개의 요청
레이스 컨디션(= 경쟁 상태)

프로레스 os

시스템 콜 >> 커널 

생성자
생성자가 언제 생성되는지?
>> class 를 정의하는 시점에서

전역변수 : 어디서나 바로 접근이 가능한 변수를 전역변수
ex) 잘변하지않는값. url 

static 이라고 정의되있는데 
static 메모리 영역에는 static 이 붙은
변수와 메서드도 다 들어갈 수 있다.

기본문법은 object 랑은 아예 관계가없으며
for문은 아예 관계가없는거임 걍 기본문법 그자체

반복문을 시작해.

필드 접근법

다형성

오버로딩:

같은 이름의 메서드를 여러 방식으로 호출할 수 있도록 다양한 버전을 제공.
**"하나를 여러 번 정의"**하는 느낌.
오버라이딩:

부모의 기능을 자식이 재정의하여 자신의 방식으로 동작하도록 변경.
**"상속받은 것을 고쳐서 사용"**하는 느낌.

서버 
웹서버와 웹어플리케이션의 차이를 알아오자

웹서버 , 서버, 아파치 , nginx
반복문 강의
